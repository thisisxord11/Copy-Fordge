import {useEffect,useRef} from 'react';
export default function Background(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current!,x=c.getContext('2d')!;let w=0,h=0;const dots=Array.from({length:90},()=>({x:Math.random(),y:Math.random(),z:.2+Math.random()*.8,s:Math.random()*1.5+.3}));const resize=()=>{w=c.width=innerWidth*devicePixelRatio;h=c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);w=innerWidth;h=innerHeight};resize();addEventListener('resize',resize);let raf=0;const draw=()=>{x.clearRect(0,0,w,h);x.fillStyle='#050505';x.fillRect(0,0,w,h);for(const d of dots){d.y-=.00015*d.z;if(d.y<0)d.y=1;x.beginPath();x.arc(d.x*w,d.y*h,d.s,0,Math.PI*2);x.fillStyle=`rgba(255,255,255,${.12*d.z})`;x.fill()}raf=requestAnimationFrame(draw)};draw();return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize)}},[]);return <canvas ref={ref} className="bg-canvas"/>}
